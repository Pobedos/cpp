#ifndef TASKS_HPP
#define TASKS_HPP

#include <cstddef>

namespace mukhamadieva
{
  int task1(const char* order);
  int task2(const char* file);
  int task3();
  int task4(const char* order, size_t size);
}

#endif
